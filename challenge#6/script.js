// question 6 Complete the solution so that it returns true if the first argument(string) passed in ends with the 2nd argument (also a string).

// Examples:

// solution('abc', 'bc') returns true
// solution('abc', 'd')  returns false

function solution(str, ending) {
  // TODO: complete

  return str.endsWith(ending);
}

// this one was a little trckier to complete because i had to google the endsWith method because i noticed my other attemps werent working
